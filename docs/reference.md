# API Reference

`OrchestrateRM` is imported as `orchestrator_rm`. The public API:

```python
import orchestrator_rm
```

### `Orchestrator`

Key methods: `add_module()`, `apply()`, `bfloat16()`, `buffers()`, `children()`

### `OrchestratorRewardModel`

Key methods: `add_module()`, `apply()`, `bfloat16()`, `buffers()`, `children()`

